extern crate valve_resource_tools;

use valve_resource_tools::*;
#[allow(unused_imports)]
use valve_resource_tools::resource::vpk::prelude::*;

fn main() -> Result<(), Box<dyn std::error::Error>> {
	Ok(())
}